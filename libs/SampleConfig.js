/* To use this sample configuration file,
 * fill in all of the fields below,
 * as all fields are required for full function.
 * After, copy this file and name it Config.js
 * and the bot will detect your config settings.
 */
var config = {};

config.discord = {};
config.plug = {};
config.reddit = {};

config.discord.token = null;

config.discord.relaychannel = null;
config.discord.voicechannel = null;

config.plug.email = null;
config.plug.password = null;

config.plug.mainroom = null;

config.reddit.user_agent = null;
config.reddit.client_id = null;
config.reddit.client_secret = null;
config.reddit.username = null;
config.reddit.password = null;

module.exports = config;